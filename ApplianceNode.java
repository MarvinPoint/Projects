public class ApplianceNode {
   
   protected Appliance data;
   protected ApplianceNode next;
   // constructor to initialize node with appliance data
   public ApplianceNode(Appliance data) {
       this.data = data;
       this.next = null;
   }
}

